# Dependencias de usb_camera_bridge.py (puente de webcam USB para pruebas -
# ver ese archivo). El microservicio garita-controller en si vive en
# backend/services/garita-controller/ (tiene su propio requirements.txt, con
# ultralytics/paho-mqtt/minio ademas de estas).
opencv-python-headless>=4.10,<5.0
fastapi>=0.115,<1.0
uvicorn[standard]>=0.32,<1.0
